export enum Modes {
    DAILY = 'daily',
    WEEKLY = 'weekly',
    MONTHLY = 'monthly',
    YEARLY = 'yearly'
}

export enum MetadataKeys {
    WEEKLY = 'weekdays',
    MONTHLY = 'monthDays',
    YEARLY = 'yearDays'
}
